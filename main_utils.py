

def read_arguments(parser):
    """
    This function took the parser passed in parameter 
    and use it to read the cmd argument and 
    return the arguments

    Args:
        parser ([type]): [description]
    """
